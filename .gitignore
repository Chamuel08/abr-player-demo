# Xcode build artifacts
DerivedData/
build/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Swift Package Manager
.swiftpm/
.build/
# 注意：不忽略 Packages/ —— ABREngine 本地包源码需要入库

# macOS
.DS_Store
*.swp

# CocoaPods (not used, but just in case)
Pods/

# 公开吞吐 trace 数据集（落盘约 125MB，用 scripts/download_traces.sh 拉取，不入库）
data/

# Python
__pycache__/
*.pyc
